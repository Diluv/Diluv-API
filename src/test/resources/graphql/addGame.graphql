mutation ($slug: String!, $name: String!, $url: String!, $logoPNG: Upload!, $logoWebp: Upload!) {
    addGame(slug: $slug, name: $name, url: $url, logoPNG: $logoPNG, logoWebp: $logoWebp) {
        name
        slug
    }
}